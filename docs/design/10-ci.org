#+title: anvil-pkg — CI integration (GitHub Actions)
#+author: zawatton
#+date: 2026-05-06
#+property: status SHIPPED

* STATUS

SHIPPED 2026-05-06.  Single-task scope.  Lands a GitHub Actions
workflow that gates =push= / =pull_request= against =main=.

Current audit note (2026-06-08): this CI baseline is now superseded by
later phases.  The current local mock suite is 182/182, Phase 4-H added
a separate =smoke.yml= workflow for real-Nix =make smoke-eval=, and
the same smoke workflow also runs =make nix-check= for the repository
flake.  The main =test.yml= gate now runs =make lint=, =make test=,
=make smoke-pairs-check=, and =make smoke-render=.  The equivalent
local aggregate target is =make check=.

Not a feature phase — infrastructure only.  Phase 4-G (private
repo) and Phase 5 (NeLisp backend) remain the next *feature*
milestones; CI is meta.

* Goal

| Today                                                  | After CI                                                        |
|--------------------------------------------------------+-----------------------------------------------------------------|
| =make test= / =make compile= run only on author's box  | Every push / PR against =main= runs them on a clean Ubuntu VM   |
| Public repo, no objective signal of build health       | README badge: ✓ if green, ✗ if red                              |
| External PR contributor has no automatic gate          | PR cannot merge with red CI; reviewer sees pass/fail at a glance|
| Regression on stale env / removed dep is silent        | =emacs -Q --batch= on a fresh container catches it on next push |

* Locked decisions

** L35 — minimum matrix: Emacs 30.x × Linux, single job

- Emacs version: =30.1= (current dev box) via =purcell/setup-emacs=
  action (de-facto standard, GPL-3-friendly).
- OS: =ubuntu-latest= only.
- Reason: =make test= is mock-driven, has no Nix dependency, and
  finishes in ~1s.  A larger matrix is overkill until we see a
  cross-version flake.  Emacs 29 / macOS / Windows can be added
  in a follow-up if a real bug report demands it.

** L36 — workflow steps

1. =actions/checkout@v4=
2. =purcell/setup-emacs@master= with =version: 30.1=
3. =make lint= (source/script/test byte-compile warnings-as-errors +
   source/script/test =check-declare=)
4. =make test= (ERT 182/182 expected in the current suite)
5. =make smoke-pairs-check= (=SMOKE_EVAL_PAIRS= /
   =SMOKE_BUILD_PAIRS= are non-empty, use =example-file:nix-attr=
   form, reference existing example files, and expose the requested
   attrs in rendered flakes; no Nix)
6. =make smoke-render= (all =examples/*.el= recipes register at
   least one package, render to =flake.nix=, and pass a flake-shape
   check, no Nix; zero examples is an error)

No caching of =.elc= — the build is fast enough that cache
invalidation cost outweighs the saving.

Local equivalent: =make check= runs the same no-Nix gate
(=make lint= + =make test= + =make smoke-pairs-check= +
=make smoke-render=) while the workflow keeps the steps split for
readable GitHub logs.  =make lint= includes runtime source, smoke
helper script, and ERT byte-compilation plus declaration checks for
all three source sets.

** L37 — triggers: push / pull_request against main

- =on.push.branches: [main]= — gate the canonical branch.
- =on.pull_request.branches: [main]= — block bad PRs at review
  time.
- No scheduled / cron run.  We're not testing against external
  drift (yet).

** L38 — no Nix smoke in CI

Nix tarball / build / cache setup is heavy (=cachix/install-nix-action=
+ store warm-up).  =examples/= recipes carry =sha256-PLACEHOLDER=
values that won't resolve, so a real-Nix smoke would fail
deterministically until we either (a) replace placeholders with
real hashes (=Real-Nix smoke= proposal), or (b) write a separate
tiny example that does build.

Decision: defer Nix-in-CI to its own phase.  This phase keeps
CI scope to the host-side Elisp gate.

Superseded by Phase 4-H and the later repository-flake gate:
=.github/workflows/smoke.yml= now installs Nix, runs =make nix-check=
for the top-level flake, and runs =make smoke-eval= against the four
real-hash examples.

** L39 — README badge

Add a single-line badge at the top of =README.org= linking to
the workflow run page:

#+begin_src org
[[https://github.com/zawatton/anvil-pkg/actions/workflows/test.yml][file:https://github.com/zawatton/anvil-pkg/actions/workflows/test.yml/badge.svg]]
#+end_src

Place above the existing =Status:= block so it's the first thing
a visitor sees.

* Open questions / non-goals

- *OQ24.* Do we want to fail CI on byte-compile warnings even
  when =make test= passes?  Decision: yes.  The current =make lint=
  gate runs source, smoke helper script, and ERT byte-compile with
  =byte-compile-error-on-warn= and checks declarations in all three
  source sets.
- *OQ25.* Should =lint= be required to pass?  Decision: yes.  The
  current local audit passes =make lint= without false positives, so
  =test.yml= runs =make lint= before =make test=.
- *OQ30.* Should example rendering be part of the no-Nix CI gate?
  Decision: yes.  =make smoke-render= is fast, does not require Nix,
  auto-discovers =examples/*.el=, and catches stale example DSL before
  the real-Nix =smoke.yml= job.
- *Non-goal.* Multi-Emacs matrix.  Adding 28/29/30 triples
  runtime + runs into setup-emacs binary availability gaps on
  older versions.  Re-evaluate when we get a 28/29 bug report.
- *Non-goal.* macOS / Windows runners.  GitHub free minutes
  multiplier (10× macOS, 2× Windows) makes this expensive
  before we have a known cross-platform issue.
- *Non-goal for this phase.* Real-Nix smoke / build cache.  Completed
  later by Phase 4-H's separate =smoke.yml=.
- *Non-goal.* Coverage upload (Codecov etc.).  The suite was 91 tests
  when this CI phase landed and is 182/182 in the current audit; no
  coverage tooling currently — additive complexity for unclear ROI.
- *Non-goal.* Auto-format / commit-message gate.  Out of scope.

* Sub-task decomposition

Single sub-task.  Touches:

- =.github/workflows/test.yml= (new, ~25 lines).
- =README.org= (badge line + Phase plan table row).

** Implementation order

1. Create =.github/workflows/test.yml= with the four steps
   above.
2. Push branch / open PR to verify CI green on first run
   (or push directly to =main= since this is the gate's
   first installation — there is nothing yet to gate against).
3. Add README badge once the workflow has at least one
   green run (so the badge is not red on the first visit).

* Test plan

Manual verification only:

- Workflow file lints with =yamllint= (informal).
- After push, GitHub Actions tab shows a green run within
  ~2 minutes.
- Intentional break (e.g., add =(error "boom")= to a test) on
  a throwaway branch confirms red CI on that branch's PR.
  Revert before merge.

* Acceptance

- =.github/workflows/test.yml= exists and validates on push.
- Current local equivalent of the workflow gate is green:
  =make check= (=make lint= + =make test= (=182/182=) +
  =make smoke-pairs-check= + =make smoke-render=).
- README badge renders.
- Future PRs cannot be merged without green CI (branch
  protection rule is *separate* — not configured by this phase,
  noted as a follow-up since it requires repo Settings access).

* Follow-up tasks (out of scope)

- Branch protection rule on =main= requiring CI green
  (Settings → Branches → Add rule).
- Emacs 29 fallback once a 29.x user reports a bug.
- Nix-in-CI smoke phase.  Completed by Phase 4-H.
- Repository flake check.  Completed: =smoke.yml= runs
  =make nix-check=, which evaluates the pinned development shell and
  builds the flake's =checks.<system>.check= derivation running
  =make check=.
- =make lint= promotion (after =check-declare= audit).  Completed:
  current local audit passes =make lint= without warnings, including
  smoke helper / ERT byte-compilation and script/test declaration
  checks, and =test.yml= runs it before =make test=.
- No-Nix example render gate.  Completed: =test.yml= runs
  =make smoke-pairs-check= and =make smoke-render= after the ERT
  suite, including smoke pair metadata / rendered-attr validation,
  generated =flake.nix= shape checks for every =examples/*.el= recipe,
  and a guard against accidentally rendering zero examples or
  package-empty examples.
